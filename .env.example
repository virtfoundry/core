# Optional local overrides (copy to .env — not used by default Go config).
# Prefer config/config.yaml for API/worker local dev (see config/config.yaml.example).

# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_USER=virtfoundry
# DB_PASSWORD=virtfoundry
# DB_DATABASE=virtfoundry
