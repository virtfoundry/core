FROM node:22-alpine AS builder
WORKDIR /app
COPY ui/package.json ui/package-lock.json ./
RUN npm ci
COPY ui/ .
RUN npx vite build

FROM nginx:1.27-alpine
COPY docker/nginx-ui.conf /etc/nginx/conf.d/default.conf
COPY --from=builder /app/dist /usr/share/nginx/html
# Grant only the low-port bind capability needed by the non-root nginx user.
RUN apk add --no-cache libcap \
	&& setcap cap_net_bind_service=+ep /usr/sbin/nginx \
	&& apk del libcap \
	&& chown -R nginx:nginx /usr/share/nginx/html \
	&& touch /var/run/nginx.pid \
	&& chown -R nginx:nginx /var/cache/nginx /var/log/nginx /var/run/nginx.pid /etc/nginx/conf.d
USER nginx
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
