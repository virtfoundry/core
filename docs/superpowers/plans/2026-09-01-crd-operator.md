# CRD operator plan (archived)

**Status:** Homelab slice complete (2026-09). Full task list removed to avoid drift.

**Canonical design:** [2026-09-01-crd-operator-design.md](../specs/2026-09-01-crd-operator-design.md)

**Shipped:** `virtfoundry/operator`, kubernetes store (`store.driver=kubernetes`), homelab E2E green, no MySQL/worker in production path.

**Remaining (track in ROADMAP / issues):** Disk/Network/VPC controllers, full hypervisor decoupling from API, operator CI digest write-back.
